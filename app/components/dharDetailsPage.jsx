/* import React, { Suspense } from "react";
import { DharDetails } from "./DharDetails";

const DharDetailsPage = () => {
  const { products } = useContext(GlobalContextcreated);

  return (
    <Suspense fallback={<div>Loading...</div>}>
      <DharDetails products={products} />
    </Suspense>
  );
};

export default DharDetailsPage */